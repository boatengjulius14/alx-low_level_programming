#include "main.h"

/**
 * print_rev - prints a string in reverse, followed by a new line
 * @s: string
 */
void print_rev(char *s)
{
	int sum;

	while (s[sum] != '\0')
	{
		sum++;
	}

	while (i >= 0)
	{
		_putchar(s[sum]);
		sum--;
	}
	_putchar('\n');
}
