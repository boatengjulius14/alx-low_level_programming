#include "main.h"

/**
 *_strlen - returns the lenth of a string
 *@s: pointer
 *return: result of the string length
 */

int _strlen(char *s)
{
	int result;

	result = strlen(s);
	return (result);
}
